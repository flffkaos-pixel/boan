# D:BOAN 학습 추적 설계

## 학습 경험

D:BOAN은 공개 학습 아카이브를 읽는 화면과 개인 학습 기록을 분리하되, 두 경험을 연결한다. `/library`의 각 프로젝트에는 실제 문서 제목으로 구성한 **실습 체크리스트**와 진행률을 표시한다. 사용자가 완료를 표시하면 로그인한 계정의 프로젝트별 기록으로 저장하고, `/study`의 **학습 현황**에서 전체 완료율·단계별 진행률·최근 활동·퀴즈 결과를 확인한다.

학습 현황은 개인 데이터가 있는 영역이므로 제공된 `DashboardLayout`의 인증 흐름을 사용한다. 로그인 전에는 내부 자료는 열람할 수 있지만, 진도·체크리스트·퀴즈 결과를 영구 저장하려면 로그인 안내를 제공한다.

## 데이터 모델

| 엔터티 | 단위 | 저장 내용 |
|---|---|---|
| `learning_progress` | 사용자 × 프로젝트 | 단계, 상태, 완료한 체크리스트 ID, 완료율, 갱신 시각 |
| `quiz_attempts` | 퀴즈 제출 1회 | 사용자, 점수, 총 문항, 답안, 제출 시각 |

프로젝트 체크리스트는 별도의 모의 데이터가 아니라 선택한 번들의 실제 학습 문서 제목에서 생성한다. 체크리스트 ID는 `프로젝트 ID:문서 경로`로 고정해 번역/원문 전환에도 동일한 진도 기록을 사용한다.

## 복습 퀴즈

한국어 보안 용어 퀴즈는 해시, HSTS, CSP, DNS, MFA, 최소 권한, SIEM, 시크릿 스캐닝처럼 현재 아카이브에서 반복되는 방어 중심 개념을 짧은 객관식으로 복습한다. 퀴즈는 즉시 정답·해설을 보여 주고 결과를 저장해 학습 현황에 반영한다.

## 콘텐츠 가시성

홈의 4단계 레일에는 실제 아카이브의 단계별 프로젝트 수를 함께 표시한다. 화면이 좁아져도 `Foundations`, `Beginner`, `Intermediate`, `Advanced` 네 레일이 동일한 4열 안에서 줄어들며, 가로 스크롤은 사용하지 않는다.
